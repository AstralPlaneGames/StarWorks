name: StarWorks CI

on: [push, pull_request]

jobs:
  windows:
    name: Windows
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: true

    - name: Install workloads
      run: |
        dotnet workload install android ios tvos --skip-manifest-update

    - name: dotnet build and pack StarWorks
      run: |
        dotnet build -c Debug StarWorks.csproj
        dotnet pack StarWorks.csproj

    - uses: actions/upload-artifact@v4
      with:
        name: ${{ github.event.repository.name }} Artifacts
        path: |
          bin/Release/*.nupkg
